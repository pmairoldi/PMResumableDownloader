#include "Pods-PMResumableDownloader.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/PMResumableDownloader" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/PMResumableDownloader"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}